pipeline {
    agent any 
     stages {
         stage("init") {
             steps {
                 echo "Initialization"
             }
         }
         stage(" Build") {
             steps {
                 echo "Build a project"
             }
         }
         stage("Deploy") {
             steps {
                 echo "Depoying an application"
             }
         }
     }
 }